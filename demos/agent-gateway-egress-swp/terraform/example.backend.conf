# GCS backend configuration for the agent-gateway-egress-swp demo.
# Copy to backend.conf and fill in the values.
# Usage: terraform init -backend-config=backend.conf

bucket = "your-project-id-tfstate"   # GCS bucket for Terraform state
prefix = "agent-gateway-egress-swp"  # state key prefix within the bucket
